<%= form_with(model: breast_feeding, local: true) do |form| %>
  <% if breast_feeding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(breast_feeding.errors.count, "error") %> prohibited this breast_feeding from being saved:</h2>

      <ul>
      <% breast_feeding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.text_field :duration %>
  </div>

  <div class="field">
    <%= form.label :baby_id %>
    <%= form.select :baby_id, options_for_select(@babies) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
