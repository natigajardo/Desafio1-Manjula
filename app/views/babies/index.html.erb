<p id="notice"><%= notice %></p>

  <table class="container margenes-tabla table table-hover text-center">
    <thead>
      <tr>
        <th class="letra-content pr-1">Nombre</th>
        <th class="letra-content pr-1">Total cantidad <br>bebida</th>
        <th class="letra-content pr-1">Total tiempo <br>bebido</th>
        <th colspan="3" class="letra-content pr-1"></th>
      </tr>
    </thead>
    
    <tbody>
      
      <% @babies.each do |baby| %>
        <tr>
          <td class="letra-content"><%= baby.name %></td>
          <td class="letra-content"><%= baby.total_drinking %></td>
          <td class="letra-content"><%= baby.total_duration %></td>
          <td class="letra-content"><%= link_to 'Ver', baby %></td>
          <td class="letra-content"><%= link_to 'Editar nombre', edit_baby_path(baby) %></td>
          <td class="letra-content"><%= link_to 'Eliminar bebé', baby, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

